<?xml version="1.0" encoding="UTF-8"?>
<server description="new server">

    <!-- Enable features -->
    <featureManager>
        <feature>restfulWS-3.1</feature>
        <feature>cdi-4.0</feature>
        <feature>jsonp-2.1</feature>
        <feature>jsonb-3.0</feature>
    </featureManager>


<!--    <logging consoleLogLevel="INFO" traceSpecification="com.ibm.ws.cdi=all:com.ibm.ws.injection=all" />-->
<!--    <logging consoleLogLevel="INFO" traceSpecification="com.ibm.ws.injection=all:com.ibm.ws.cdi=all" />-->
<!--    <logging traceSpecification="org.jboss.weld=all:com.ibm.ws.jpa=all" />-->


    <library id="log4jConfig">
        <folder dir="/src/main/resources" />
    </library>


    <classloader commonLibraryRef="log4jConfig"/>


    <basicRegistry id="basic" realm="BasicRealm"></basicRegistry>

    <!-- To access this server from a remote client add a host attribute to the following element,
    e.g. host="*" -->
    <httpEndpoint id="defaultHttpEndpoint"
        httpPort="9080"
        httpsPort="9443" />

    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true" />

    <!-- Configures the application on a specified context root -->
    <webApplication contextRoot="/" location="salary-tracker.war"  />


    <!-- Default SSL configuration enables trust for default certificates from the Java runtime -->
    <ssl id="defaultSSLConfig" trustDefaultCerts="true" />
</server>